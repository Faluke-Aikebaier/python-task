#1 Write a program that inputs marks of a student and displays "Pass" if marks are more than 40 and "Fail" otherwise by using conditional expressions or python ternary operator.

def grade(x):
  if x >= 40:
    print ("Pass")
  else:
    print("Fail")
grade(50)
grade(10)